name: Build Windows Portable

on:
  # Trigger manual dari tab "Actions" di GitHub
  workflow_dispatch:
    inputs:
      version:
        description: 'Versi build (misal: 1.0.0)'
        required: false
        default: 'dev'

  # Juga trigger otomatis saat push tag v*.*.* (opsional)
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-windows:
    name: Build PyInstaller (Windows x64)
    runs-on: windows-latest

    steps:
      # ── 1. Checkout source code ────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Setup Python 3.11 ──────────────────────────────────────────────
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          architecture: 'x64'

      # ── 3. Cache pip packages (percepat build ulang) ──────────────────────
      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # ── 4. Install dependencies ───────────────────────────────────────────
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        shell: cmd

      # ── 4b. Verifikasi semua local package dapat diimport ─────────────────
      # Jika langkah ini gagal, build.spec PASTI gagal mengemas modul tersebut.
      - name: Verify local package imports
        run: |
          python -c "import sys; sys.path.insert(0, '.'); import core.compare_engine; import core.normalization_engine; import workers.compare_worker; import models.compare_config; import config.constants; print('All local imports OK')"
        shell: cmd

      # ── 5. Jalankan PyInstaller ───────────────────────────────────────────
      # PYTHONPATH wajib di-set agar PyInstaller analysis phase dapat
      # mengimport semua local packages (core, models, workers, dll.).
      # pathex di build.spec sudah di-set, tapi env PYTHONPATH adalah
      # cara paling andal di Windows runner GitHub Actions.
      - name: Build with PyInstaller
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          pyinstaller build.spec --noconfirm
        shell: cmd

      # ── 6. Siapkan struktur portable ──────────────────────────────────────
      - name: Prepare portable structure
        run: |
          copy /y "_portable" "dist\SFACompareTool\_portable"
          if not exist "dist\SFACompareTool\AppData" mkdir "dist\SFACompareTool\AppData"
          if not exist "dist\SFACompareTool\AppData\logs" mkdir "dist\SFACompareTool\AppData\logs"
        shell: cmd

      # ── 7. Tentukan nama versi ─────────────────────────────────────────────
      - name: Set version string
        id: version
        run: |
          if ("${{ github.event_name }}" -eq "push") {
            $ver = "${{ github.ref_name }}"
          } else {
            $ver = "${{ github.event.inputs.version }}"
            if (-not $ver) { $ver = "dev" }
          }
          echo "VER=$ver" >> $env:GITHUB_OUTPUT
        shell: pwsh

      # ── 8. Zip hasil build ─────────────────────────────────────────────────
      - name: Zip portable folder
        run: |
          $ver = "${{ steps.version.outputs.VER }}"
          $zipName = "SFACompareTool-$ver-windows-portable.zip"
          Compress-Archive -Path "dist\SFACompareTool\*" -DestinationPath $zipName -Force
          echo "ZIP_NAME=$zipName" >> $env:GITHUB_OUTPUT
        id: zip
        shell: pwsh

      # ── 9. Upload artifact (dapat didownload dari tab Actions) ─────────────
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: SFACompareTool-${{ steps.version.outputs.VER }}-windows-portable
          path: ${{ steps.zip.outputs.ZIP_NAME }}
          retention-days: 30

      # ── 10. Buat GitHub Release jika trigger dari tag ──────────────────────
      - name: Create GitHub Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.zip.outputs.ZIP_NAME }}
          name: SFA Compare Tool ${{ steps.version.outputs.VER }}
          body: |
            ## SFA Compare Tool ${{ steps.version.outputs.VER }}

            ### Download
            Unduh file zip di bawah, ekstrak, lalu jalankan `SFACompareTool.exe`.

            ### Catatan
            - Portable — tidak perlu install, data tersimpan di folder `AppData\` di samping `.exe`
            - Membutuhkan Windows 10/11 64-bit
          draft: false
          prerelease: false
